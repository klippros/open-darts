{
  "name": "@open-darts/match-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "wrangler types --check && tsc --noEmit && tsc --noEmit -p test/tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@open-darts/game": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.5",
    "@types/node": "^24.13.2",
    "typescript": "~6.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.37.1"
  }
}
